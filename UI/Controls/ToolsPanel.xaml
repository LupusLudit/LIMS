<UserControl x:Class="LIMS.UI.Controls.ToolsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LIMS.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderBrush="#444" BorderThickness="0,1,0,0">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10">

                <TextBlock Text="Tools" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>

                <Expander Header="Watermark" IsExpanded="True">
                    <StackPanel Margin="10">
                        <CheckBox Content="Enable Watermark" />

                        <Label Content="Watermark File:" Margin="0,10,0,0"/>
                        <Button Content="Browse..." Width="120" />

                        <Label Content="Opacity:" Margin="0,10,0,0"/>
                        <Slider Minimum="0" Maximum="1" Value="0.7"/>
                    </StackPanel>
                </Expander>

            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
